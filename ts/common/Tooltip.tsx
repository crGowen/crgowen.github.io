/*import { ReactNode } from "react";

export default function Tooltip(props: {children: ReactNode; text: string; }) {
    const {children, text} = props;

    return (
        <div className="tooltipContainer">
            <div className="tooltip">{text}</div>
            {children}
        </div>
    );
}*/